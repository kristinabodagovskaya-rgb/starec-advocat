# Контекст сессии 26-27.01.2026

## Проект
Starec-Advocat 2.0 — система для работы с уголовными делами, извлечение документов из PDF с помощью Claude Vision Sonnet.

---

## Что было сделано 26.01.2026

### 1. Анализ ошибок извлечения (Том 1)

| Документ | Ошибка | Причина |
|----------|--------|---------|
| #19, #21, #24 | Неверная дата | Рукописная дата не распознана, Sonnet взял дату из текста |
| #25 | Дата из текста | Дата документа отсутствует, взята дата события |
| #27 | Ложный документ | Продолжение документа #26 (только подписи, без заголовка) |
| #62 | Искажённая фамилия | "Кавериной" вместо "Каверзину" |

### 2. Обновление системного промпта

Добавлены правила:
- ДАТА: только из заголовка или подписи, НЕ из текста
- ФАМИЛИИ: согласование рода с именем рядом
- ПРОДОЛЖЕНИЕ: нет заголовка/шапки = продолжение

### 3. Оптимизация промпта

- Сжат на 47% (1499 → 898 символов)
- Экономия ~$0.50 на 328 страниц

### 4. Исправление бага

`'NoneType' object has no attribute 'strip'` — Claude иногда возвращает `title: null`

Исправлено: `(result.get("title") or "Документ").strip()`

### 5. Коммит

```
09c3efd - feat(extraction): улучшение извлечения документов и оптимизация промпта
```

---

## Что было сделано 27.01.2026

### 1. Анализ Тома 2

**Ошибка:** Документы 17 и 18 разделены, хотя это один документ.
- 17. Учетная карточка ЗАО «Ниеншанц» — стр. 35-38
- 18. Справка о видах деятельности — стр. 39-41

**Должно быть:** Документ 17 — стр. 35-41 (единая таблица)

**Причина:** Sonnet не понял что таблица продолжается.

### 2. Добавлено правило про таблицы

```
Если таблица не закончилась — это продолжение!
```

### 3. Анализ Тех условий (документы 79-84)

Sonnet разбил на 6 документов:
- 79. ТУ Лист утверждения
- 80. Технические условия
- 81. Приложение А
- 82. Приложение Б
- 83. Приложение В
- 84. Лист регистрации изменений

**Вывод:** Sonnet действовал по инструкции (каждое приложение имеет заголовок). В описи это один документ, но для навигации детальная разбивка удобнее. Решено оставить как есть.

---

## Текущий системный промпт

```
Анализ страниц уголовного дела. Определи границы документов.

ПРАВИЛА:
1. ОПИСЬ = таблица |№|Наименование|Листы| — это ОДИН документ, не список отдельных!

2. НАЧАЛО документа (is_start=true): заголовок вверху (ПОСТАНОВЛЕНИЕ/ПРОТОКОЛ/РАПОРТ) ИЛИ шапка организации ИЛИ номер/дата исх.

3. ПРОДОЛЖЕНИЕ (is_start=false): нет заголовка/шапки вверху, только текст/таблицы/подписи. Если таблица не закончилась — это продолжение!

4. ДАТА: только из заголовка или подписи! НЕ из текста. Неразборчивая/рукописная → date=""

5. ФАМИЛИИ: мужское имя рядом → муж.фамилия (-ов/-ин), женское → жен. (-ова/-ина)

6. Помни контекст: опись продолжается пока нет нового заголовка.

7. НАЗВАНИЯ: незнакомое/редкое слово — читай внимательно по буквам!

JSON: {is_start, is_end, is_opis, type, title, date}
```

---

## Файлы изменённые в этой сессии

- `backend/app/api/v1/cases.py` — системный промпт, обработка title
- `backend/app/models/case.py` — модель ExtractionRun
- `backend/app/models/__init__.py` — экспорт ExtractionRun
- `frontend/src/pages/cases/PDFViewerPage.tsx` — SSE прогресс, скролл

---

## Открытые вопросы

1. **Приложения** — оставлены как отдельные документы (удобнее для навигации)
2. **Правило про таблицы** — добавлено, требует проверки на Томе 2
3. **"Ниеншанц"** — правило про внимательное чтение добавлено

---

## Бэкенд

Статус: работает на `localhost:8000`
Последний перезапуск: 27.01.2026 12:35

# 🛑 РЕПОЗИТОРИЙ DEPRECATED

> Этот репозиторий **БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ** для разработки STAREC.

## Где actual код

Production ecosystem-backend STAREC лежит в:
**`Starec-net/advocat-starec-3`** (ветка `ecosystem`)

Это и есть источник правды, который деплоится на `advocat-prod` (контейнер `starec3-backend`).

## Что было в этом репо

Старая версия OCR-сервиса с TODO-заглушками в `backend/app/api/auth.py`,
`backend/app/api/v1/strategy.py` и пр. Не соответствует текущей архитектуре,
описанной в каноне → [STAREC_Архитектура_канон.md](https://github.com/Starec-net/starec-desktop/blob/main/docs/STAREC_Архитектура_канон.md) (на репо `Starec-net/starec-desktop`).

## Что делать если случайно попал сюда

1. Закрыть.
2. Идти в `Starec-net/advocat-starec-3` (ветка `ecosystem`).

Помечено DEPRECATED 2026-05-15 после полного аудита (SOT-3 в task-tracker).
